#include "list.h"

void init_pattern(t_pattern pattertn)
{
	int i;
	pattern.cur_row = 0;
	pattern.row[0].cur_elem = 0;
	while (i < )
	{

	}
}

int push_in_pattern(char c, t_pattern pattern)
{
	if (patttern->)
}

int decrease_pattern(pattern)
{
}

char *get_pattern()
{
	char c;
	char c_prev;
	int x;
	int y;
	int weight;
	int height;
	t_pattern pattern;

	init_pattern(pattern);

	s = 1;
	x = 0;
	y = 0;
	while (read(0, &c, 1))
	{
		if (c != '\n')
		{
			push_in_pattern(c, pattern);


		}
			if (width > 0 && width != x + 1) //be careful - the last string consits of \n only
				return ("");
			x = 0;
			y++;
			cut_pattern(); // cut 6-8 elements if they are equal to 3-6 elements (if length 
			//c_prev = 
		}
		else
		{
			if (x > 0 

		}
	}
}

int		main(void)
{
	char *pattern;
	
	pattern = get_pattern();

	//buf[s] = '\0';
	//ft_putstr(buf);:q
	return (0);
}
